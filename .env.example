# Monero MCP Server – environment variables
# Copy to .env and set values. Do not commit .env or real secrets.

# -----------------------------------------------------------------------------
# Wallet RPC connection (monero-wallet-rpc)
# The server connects to wallet RPC at http://MONERO_RPC_HOST:MONERO_RPC_PORT/json_rpc
# -----------------------------------------------------------------------------

# Host of the running monero-wallet-rpc instance (e.g. 127.0.0.1 or host.docker.internal when agent runs in Docker)
MONERO_RPC_HOST=127.0.0.1

# Port of monero-wallet-rpc (default: 18082 mainnet, 38082 stagenet, 28082 testnet; use 18083 if using this repo's docker compose)
MONERO_RPC_PORT=18082

# Optional: RPC username if wallet-rpc was started with --rpc-login (HTTP Digest auth)
MONERO_RPC_USER=

# Optional: RPC password for --rpc-login (leave empty if wallet-rpc uses --disable-rpc-login)
MONERO_RPC_PASS=

# -----------------------------------------------------------------------------
# Network
# -----------------------------------------------------------------------------

# Monero network: mainnet, stagenet, or testnet (must match wallet-rpc and daemon)
MONERO_NETWORK=mainnet

# -----------------------------------------------------------------------------
# Transfers (sending XMR)
# -----------------------------------------------------------------------------

# Set to true to allow transfer, sweep_all, confirm_transfer; false for read-only (balance, address, etc.)
MONERO_ALLOW_TRANSFERS=false

# Optional: max XMR per single transfer (e.g. 1.0). No limit if unset.
MONERO_MAX_TRANSFER_AMOUNT=

# Optional: max total XMR sent per rolling 24h. No limit if unset.
MONERO_DAILY_LIMIT_XMR=

# Minimum seconds between successful transfers (0 = no cooldown)
MONERO_TRANSFER_COOLDOWN_SECONDS=60

# Optional: comma-separated allowlist of destination addresses; if set, transfers to any other address are rejected
MONERO_ALLOWED_ADDRESSES=

# If true, transfer returns a confirmation token; confirm_transfer must be called to execute (recommended)
MONERO_REQUIRE_CONFIRMATION=true

# -----------------------------------------------------------------------------
# Audit and logging
# -----------------------------------------------------------------------------

# Optional: path to JSONL file for auditing tool calls (e.g. ./monero-mcp-audit.jsonl)
MONERO_AUDIT_LOG_FILE=./monero-mcp-audit.jsonl
